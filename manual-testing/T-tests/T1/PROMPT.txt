Create a new file at `src/status.ts` containing exactly:

export const status = "ready";

Use the `write_file` tool.
Do not make unrelated edits.
Before finishing, verify the file content and exact byte length.

Your final answer must be exactly:

verified=yes
file=src/status.ts
bytes=31
